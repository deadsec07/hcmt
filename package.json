{
  "name": "hcmt",
  "version": "1.0.0",
  "scripts": {
    "clean": "hardhat clean",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy-local.js --network localhost",
    "deploy:testnet": "hardhat run scripts/deploy-testnet.js --network skaleTestnet",
    "deploy:prod": "hardhat run scripts/deploy-prod.js --network skale"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.21.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0",
    "dotenv": "^16.0.0",
    "ethers": "^5.8.0"
  }
}
